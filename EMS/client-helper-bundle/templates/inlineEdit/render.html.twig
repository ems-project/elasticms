{%- block header -%}
    <strong>Inline Editor</strong>
    <div class="editor-actions">
        <button class="btn" id="btn-edit"><i class="fas fa-pen"></i> Edit</button>
{#            <a href="{{ url }}" class="btn"><i class="fas fa-close"></i> Close</a>#}
    </div>
{%- endblock header -%}

{%- block sidebar -%}
    {%- if payload is defined -%}
        {{ dump(payload) }}
    {%- endif -%}
{%- endblock sidebar -%}

