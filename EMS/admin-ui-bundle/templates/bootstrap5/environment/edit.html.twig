{% extends '@EMSAdminUI/bootstrap5/base/admin.html.twig' %}{% trans_default_domain 'emsco-environment' %}

{% block title %}{{ 'environment.edit.title'|trans({ '%name%': environment.name }) }}{% endblock %}
{% block pagetitle %}{{ block('title') }}{% endblock %}
{% block breadcrumb %}
	<ol class="breadcrumb">
		<li class="breadcrumb-item"><i class="fa fa-database"></i> {{ 'environment.plural'|trans }}</li>
		<li class="breadcrumb-item"><a href={{ path('emsco_admin_environment_index') }}>{{ 'environment.index.title'|trans }}</a></li>
   		<li class="breadcrumb-item active">{{ 'environment.edit.short_title'|trans }}</li>
   	</ol>
{% endblock %}

{% block body %}
<div class="row">
	<div class="col-md-12">
		<div class="card card-{{ theme_color }} card-outline">
			<div class="card-header">
				<h3 class="card-title">{{ block('title') }}</h3>
			</div>
			{{ form_start(form) }}
				<div class="card-body">
					<div class="row">
						<div class="col-md-4">
							{{ form_row(form.name) }}
							{{ form_row(form.label) }}
							{{ form_row(form.color) }}
							{{ form_row(form.rolePublish) }}

							{% if form.circles is defined %}
								{{ form_row(form.circles) }}
							{% endif %}
						</div>
						<div class="col-md-8">
							{{ form_row(form.inDefaultSearch) }}
							{{ form_row(form.updateReferrers) }}
							{{ form_row(form.baseUrl) }}
							{{ form_row(form.templatePublication) }}
						</div>
					</div>

				</div>
				<div class="card-footer">
					<div class="btn-group">
						{{ form_widget(form.save) }}
					</div>
				</div>
			{{ form_end(form) }}
		</div>
	</div>
</div>
{% endblock %}
