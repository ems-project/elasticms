{% extends '@EMSAdminUI/bootstrap5/template/card.html.twig' %}

{% block footer %}
    {% set footer %}
        <div class="btn-group">
            <button type="button" class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#edit_field_type_fieldType_modal_add">
                    <i class="fa fa-plus"></i> Subfield
            </button>
            {{ form_widget(form.save) }}
            {{ form_widget(form.saveAndClose) }}
        </div>
    {% endset %}
    {{ parent() }}
{% endblock footer %}
