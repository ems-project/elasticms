{% extends '@EMSCH/template/page/card/page.html.twig' %}


{% block description %}
    {% if target.source.(locale).title|default(false) %}
        <h3 class="h2 news-heading">{{ target.source.(locale).title }}</h3>
    {% endif %}
    {{ target.source.(locale).summary|default('')|emsch_routing }}
{% endblock description %}
